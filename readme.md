# Loan application homework

## Running the application
To run the application from command line (if `docker` is present on system)
```
docker-compose up --build
```

Or run locally as a Spring boot project.
